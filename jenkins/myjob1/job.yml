---
# Variables
- include_vars: jenkins/myjob1/variables.yml
- include_vars: jenkins/myjob1/secrets.yml

- name: Create job in Jenkins
  jenkins_job:
    config: "{{ lookup('template', 'templates/config.xml.j2') }}"
    name: Auto_Test
    user: "{{ jenkins_username }}"
    token: "{{ jenkens_token }}"

